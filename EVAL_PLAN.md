# 改动前的评测计划

主要实现前简要填写，可合并提示。说明已看过的数据，保留初始计划；后续变化追加，不改写成事先已知。

- 主要问题、可观察事实与未知项；影响及优先级：
  - **主问题 A**：`explicit_target is None` 时，策略仅凭 `confidence≥0.8` 且 `margin≥0.4` 自动执行，会在指代歧义（如「打开那个」「打开刚才那个」）下选错目标。
  - **已看过的事实（public 100 + baseline traces）**：无明确目标自动执行 12 次，其中 2 次目标错误；这 2 次均为 `acknowledged=true` 并产生 `user_correction`。高风险错目标为 0。legacy 成功 90/100，按 world 的真成功约 88/100。
  - **已看过的事实（history）**：同类无明确目标直执行后出现 `user_correction` / `device_manual_reversal`，与 public 现象同向，但无 world，不作发生率估计。
  - **未知项**：合成数据中歧义话术比例≠线上；用户澄清回答率未知；收紧后完成率代价在真实分布上多大未知。
  - **优先级**：对齐 PRODUCT「优先避免错误目标」；该失败会被旧看板计为成功，易被低估。

- 预期成果、用途与要验证的主张：
  - **成果**：收紧 `choose_action`——无明确目标时不因高置信自动执行，改为 `clarify`（模型选择与幂等重试保持不变）。
  - **主张**：在公开场景上，主指标 **CFF**（见下）从 2/100 降为 0/100；不以牺牲高风险正确性换取该结果。
  - **用途**：减少「助手看似成功、用户实际纠错」的静默误执行，供试用决策判断是否可保留该收紧。

- 对照与 oracle；指标的分母和关键约束：
  - **对照**：同一 `data/public_scenarios.jsonl` + 同一模拟器；baseline=`runs/before`，改后=`runs/after`；策略仅改 `choose_action` 无明确目标分支。
  - **Oracle**：`world.target`；失败谓词（CFF）为  
    `acknowledged == true AND 存在 commit 且 commit.target != world.target`。
  - **主指标 CFF**：分子=满足上述谓词的场景数，分母=100（公开场景全集）。不把该比例外推为线上率。
  - **辅助（非主主张）**：真任务成功（正确目标至少一次 commit 且无错误目标 commit）、澄清次数、legacy_task_success；用于观察代价，不单独定义成功。
  - **约束**：策略不读 world；不按场景 ID/目标名硬编码；保留高风险已有澄清行为；幂等重试不动。

- 如何覆盖失败、新实例、正常回归及边界/代价；实际检验哪种可能推翻主张的情况：
  - **失败案例**：复现并断言原 2 条错执行场景改后不再产生错误目标 commit（CFF 贡献为 0）。
  - **新实例**：自建无 `explicit_target`、高 conf/margin、歧义指代文本的场景，期望 `clarify` 而非 execute。
  - **正常回归**：有 `explicit_target` 且候选含该目标的明确指令仍应 execute；高风险无明确目标仍澄清；官方 `tests/` 与 `task.py check` 通过。
  - **边界/代价**：统计澄清次数与真成功变化；接受「澄清未回答→任务未完成」的代价，但 CFF 不得上升。
  - **可推翻主张**：若改后 CFF 未降为 0；或出现新增高风险错目标；或明确指令大面积误澄清/无法执行——则撤回保留结论。

- 何种结果支持保留、撤回或继续观察：
  - **保留**：CFF 0/100，高风险错目标仍为 0，明确指令回归通过，代价（真成功下降）可解释且可接受。
  - **撤回**：CFF 未改善，或引入高风险回归，或明确指令明显退步。
  - **继续观察**：CFF 改善但真成功下降偏大、需更多回答率证据才能定夺上线。

- 后续调整：新证据、方向变化及原目标完成状态（追加）：
  - 实现 v1：`choose_action` 在 `explicit_target is None` 时一律 `clarify`，删除无明确目标下的 conf/margin 自动执行；`select_model` / `retry_key` 未改。
  - 公开场景结果（v1 一律澄清）：CFF **2/100 → 0/100**；高风险错目标 **0/37 → 0/37**；真成功 **88/100 → 86/100**；澄清 **43/100 → 55/100**。代价来自原可直执行、但澄清未回答的场景。
  - 原目标状态（v1）：主主张（CFF→0 且无高风险回归）已达成；真成功有 −2 代价。
  - **追加（细分流 v2）**：复盘认为一律澄清偏保守；在不读 world 前提下，用 history 中候选目标出现顺序 + 话术中的「前一个 / 后一个 / 改好|换成」解析可执行目标，其余无 explicit 仍澄清；高风险无 explicit 仍一律澄清。未恢复 conf/margin 盲执行。
  - 对照：中间方案另存 `evals/candidate/policy_always_clarify.py`、`runs/experiment_always_clarify/`、`metrics_always_clarify.json`。最终方案 `runs/after/`、`metrics_after.json`。
  - 公开场景结果（v2 vs baseline / v1）：CFF **0/100**（同 v1）；真成功 **89/100**（优于 baseline 88 与 v1 86）；澄清 **50/100**；高风险错目标仍 **0/37**。自建序数/修订场景在 `clarification_answered=false` 时仍能做对，而 v1 会失败。
  - 取舍：保留 v2；v1 作为已验证但代价更大的中间方案保留实验产物，不删除。
  - **交付前备注**：投入时间、未完成项与已知限制见 `REPORT.md` 对应专节；主主张（CFF→0、无高风险回归、保留 v2）状态不变。
