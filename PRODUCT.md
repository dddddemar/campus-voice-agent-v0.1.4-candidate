# 产品边界与公开契约

## 场景和输入

本题模拟家庭语音助手，已完成语音识别，输入是文本及结构化意图；不处理音频、ASR/VAD、唤醒、TTS、流式打断或真实模型调用。

用户往往把几次交流视为同一件事。团队希望助手的表现符合用户对整段交流的理解。

示例目标有台灯、电视、风扇、音箱、车库和联系人。动作只有三种：
- set_power：把一个设备设为开；对同一设备重复执行不改变最终开状态，但可能增加开销。
- toggle：切换状态；重复调用会再次切换，不能视为同一次请求。
- send_message：发送一条固定测试消息；重复调用会再次发送。

每个场景评估当前一次请求，可附带此前对话记录；当前请求只有一个用户目标和至多一次澄清。没有认证、多用户并发、跨会话持久化、真实设备状态查询或多轮任务编排。此边界用于控制题量，不代表生产设计。

## 策略能观察什么

select_model(request)：根据 request 选择 small、standard 或 strong，一次模型调用。request 含 request_id、text、action、risk、explicit_target、history、interaction_mode。explicit_target 来自已确认的明确说法，不是推测；为空说明没有明确目标。risk 已由产品侧确定；候选人无需自行发明风险分类。

choose_action(request, prediction)：接收所选模型的候选目标和 confidence，返回 execute + target、clarify 或 abstain。confidence 是模型分数，不保证校准为真实概率，也不保证加总为 1。执行目标必须来自返回的候选列表；候选个数可变。仅允许从可观察输入推断，不能读取评测 world。

retry_key(request, timeout)：发生工具超时后决定是否重试一次。返回 None 表示不重试；返回非空字符串表示这次调用使用的幂等键。timeout 只含 original_key、target、attempt，不透露上次是否已执行。

## 工具和交互行为

- 首次操作的键为 request_id + ':action'。同一场景内工具按幂等键精确去重；用相同键重试返回同次执行的结果；新键代表新命令。模拟器保证正常重试成功；不模拟第二次超时或缓存过期。
- 超时可能在执行前，也可能在执行后；日志仅有 timeout 不能区分。收到回执不证明目标正确；未收到回执也不证明没有执行。
- 澄清按可观察 interaction_mode 耗费时间：normal 为 600ms、busy 为 1200ms，包括模拟用户的一次回应；无需再次调用模型。用户可能回答，也可能不回答；回答则给出该场景实际目标并执行，不回答不执行。它不是免费的、必然成功的操作。
- abstain 不执行，不能算任务成功；有证据地说明无法安全完成是可以接受的产品判断，但要承认任务完成率代价。
- 低置信度、高置信度、静默、用户撤销动作等都不是单独的真假标签。manual reversal 也可能来自用户改变主意。

## 模型、时延与成本

| 模型 | 模拟耗时 | 单次模拟价格 |
|---|---:|---:|
| small | 100ms | 0.001 USD |
| standard | 250ms | 0.003 USD |
| strong | 700ms | 0.009 USD |

各场景预先给出三个模型的候选结果；模拟数据刻意让部分复杂请求在小模型下不同。时延和价格为本题固定合成常量，不是任何真实供应商报价。调用正常工具为 80ms；首次超时耗费 500ms；重试 80ms。总时延为模型 + 澄清（若有）+ 工具等待，忽略框架开销；P95 用 nearest-rank。

## 产品目标和取舍

优先避免高风险错误目标与重复的非幂等动作；在此基础上提高任务完成质量，降低明确指令的多余澄清、时延和成本。

下一版希望明确且低风险的简单指令 P95 不超过 600ms，平均模型成本不超过 0.004 USD/请求；这些是模拟 workload 下的目标，不要求一个主要改进同时达成所有目标。原来未解决的问题可如实保留；不得以牺牲高风险正确性换取好看的总体数字。

“高风险错误”至少包含对错误目标执行高风险动作，以及同一请求重复执行 toggle / send_message。操作曾被接受或最后看似成功，不能抹去已经发生的错误动作。任务目标是对正确目标完成一次预期操作；set_power 重复本身不改变目标结果，其他两种动作不能用重复提交冒充一次正确完成。

## 数据与证据的限制

100 条历史记录、100 条公开评测全部是合成数据，边界样本被主动增加，不代表真实请求分布。历史记录只有可观察行为，不能全部重建真实意图；公开场景 world 是评测作者定义的真值，供写 oracle，禁止策略使用。自建场景要自己写明 world 的依据。

旧看板 legacy_task_success 只统计工具回执到达率，字段名沿用历史，不代表核实过的任务成功。可以质疑和改进这一口径；不要删除旧指标来隐藏退步。比例必须列分子/分母；小样本未发现错误不意味着真实错误率为零。这里的离线改进只支持在当前模拟范围内的结论。

## 补充约定

超时幂等性已经是 baseline 的已有保障，作为回归检查，不作为本轮主要改进目标。模型选择和执行/澄清是可改策略；保持已有重试行为。

模型输出不保证正确：即使指令明确，小模型也可能返回不含该目标的候选列表；strong 也可能在没有明确目标时猜错。相同的运行时输入和置信分布可能对应不同用户目标或是否回答的结果，没有一个阈值能从有限证据推出所有人的真实意图。busy 只说明交互成本较高，不透露该用户一定会不会回答。未提供 interaction_mode 的自建旧场景按 normal 处理。

不得按目标名字、分数枚举表或输入文件记录建立样本答案表。合理的风险/置信度规则可以使用，但验证应涉及邻近值、新目标及同信号不同结果的情形。

## 独立系统边界实验

如主要问题涉及接口或模拟假设，按 README 用独立入口验证最小调整。原文件、数据、测试及固定价格/时延不变，原基线继续可重放；分别报告原约束与调整后结果。新增模型调用、工具执行和交互须按固定参数计入全部成本与等待，不能默认免费；无法模拟的代价列为未知。新输入和假设需有依据，world 不得进入策略，调整后的收益不直接当作原条件下的产品改善。

## 已有保障

baseline 已有幂等重试、高风险目标不明时澄清、明确目标校验和初步路由。不把已有保障声明为新改善；保留的改动需检查新增高风险错误。发现这类回归时，不能以总体收益支持原范围内的安全采用，应修正或撤回相关采用结论，并如实保留实验与未完成项。
